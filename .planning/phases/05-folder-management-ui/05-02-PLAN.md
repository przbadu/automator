---
phase: 05-folder-management-ui
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/hooks/useDocuments.ts
  - frontend/src/components/documents/DocumentUpload.tsx
  - frontend/src/components/documents/DocumentList.tsx
  - frontend/src/components/documents/DocumentsLayout.tsx
  - frontend/src/components/documents/FolderTreeItem.tsx
  - frontend/src/components/documents/FolderTree.tsx
  - frontend/src/components/documents/MoveToFolderDialog.tsx
autonomous: false
requirements:
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Uploading a file while a folder is selected places the file in that folder"
    - "User can move a document to a different folder via context menu"
    - "User can move a folder to a different parent via context menu"
    - "Moved items appear in their new location after the operation"
    - "Tree and document list refresh after move/upload operations"
  artifacts:
    - path: "frontend/src/hooks/useDocuments.ts"
      provides: "Upload with folder_id, move document function"
      exports: ["useDocuments"]
    - path: "frontend/src/components/documents/DocumentUpload.tsx"
      provides: "Upload component accepting folderId"
    - path: "frontend/src/components/documents/MoveToFolderDialog.tsx"
      provides: "Dialog to select destination folder for move operations"
      exports: ["MoveToFolderDialog"]
    - path: "frontend/src/components/documents/DocumentList.tsx"
      provides: "Document rows with right-click context menu for move"
  key_links:
    - from: "frontend/src/components/documents/DocumentUpload.tsx"
      to: "/documents/upload"
      via: "FormData with folder_id field"
      pattern: "folder_id"
    - from: "frontend/src/hooks/useDocuments.ts"
      to: "/documents/{id}/move"
      via: "fetchWithAuth PATCH"
      pattern: "documents.*move"
    - from: "frontend/src/components/documents/DocumentsLayout.tsx"
      to: "useFolders.moveFolder"
      via: "context menu move action"
      pattern: "moveFolder"
---

<objective>
Wire upload-to-folder, document move, and folder move operations into the UI.

Purpose: Enable users to organize documents into folders via upload targeting (UI-03), document moves (UI-04), and folder moves (UI-05).
Output: Upload respects selected folder, context menus enable move operations, MoveToFolderDialog for destination selection.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-folder-management-ui/05-RESEARCH.md
@.planning/phases/05-folder-management-ui/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/src/types/index.ts (after Plan 01):
```typescript
export interface Document {
  id: string
  user_id: string
  filename: string
  file_size: number
  mime_type: string
  status: "pending" | "processing" | "converting" | "chunking" | "extracting_metadata" | "embedding" | "completed" | "failed"
  chunk_count: number
  error_message: string | null
  content_hash: string | null
  metadata: Record<string, unknown> | null
  folder_id: string | null
  created_at: string
  updated_at: string
}

export interface FolderTreeNode {
  id: string
  name: string
  path: string
  children: FolderTreeNode[]
  document_count: number
}
```

From frontend/src/hooks/useFolders.ts (created in Plan 01):
```typescript
export function useFolders(): {
  tree: FolderTreeNode[]
  selectedFolderId: string | null
  setSelectedFolderId: (id: string | null) => void
  loading: boolean
  loadTree: () => Promise<void>
  createFolder: (name: string, parentId: string | null) => Promise<Folder>
  renameFolder: (id: string, name: string) => Promise<void>
  deleteFolder: (id: string) => Promise<void>
  moveFolder: (id: string, newParentId: string | null) => Promise<void>
}
```

Backend APIs (already implemented):
```
POST   /documents/upload      file + folder_id? (FormData)  -> DocumentResponse
PATCH  /documents/{id}/move   { folder_id }                 -> DocumentResponse
PATCH  /folders/{id}/move     { parent_id }                 -> FolderResponse
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upload-to-folder and document move operations</name>
  <files>
    frontend/src/hooks/useDocuments.ts
    frontend/src/components/documents/DocumentUpload.tsx
    frontend/src/components/documents/DocumentList.tsx
    frontend/src/components/documents/MoveToFolderDialog.tsx
  </files>
  <action>
1. **Modify `useDocuments.ts`:**
   - Change `uploadDocument` signature to accept optional `folderId`: `uploadDocument(file: File, folderId?: string | null) => Promise<UploadResult>`
   - In `uploadDocument`, if `folderId` is provided and not null, append `folder_id` to the FormData: `formData.append("folder_id", folderId)`
   - Add `moveDocument(docId: string, folderId: string | null) => Promise<void>`:
     - PATCH `/documents/${docId}/move` with body `{ folder_id: folderId }`
     - On success, update the document's folder_id in local state: `setDocuments(prev => prev.map(d => d.id === docId ? { ...d, folder_id: folderId } : d))`
   - Return `moveDocument` alongside existing returns

2. **Modify `DocumentUpload.tsx`:**
   - Add `folderId?: string | null` to Props interface
   - Change `onUpload` prop type to `(file: File, folderId?: string | null) => Promise<UploadResult>`
   - In `handleFiles`, pass folderId to onUpload: `await onUpload(file, folderId)`
   - Show a small indicator when uploading to a folder: below the drop zone text, if folderId is set, show "Uploading to: {folderName}" in muted text (add `folderName?: string` to props)

3. **Create `MoveToFolderDialog.tsx`:**
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `tree: FolderTreeNode[]`, `currentFolderId: string | null`, `onMove: (targetFolderId: string | null) => Promise<void>`, `itemName: string`
   - Uses shadcn `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogFooter`
   - Shows a simplified folder tree for destination selection (recursive list with indentation, no context menus)
   - Include an "Unfiled (root)" option at the top
   - Highlight current folder (disabled/greyed out -- can't move to same location)
   - "Move" button calls onMove with selected target folder ID, then closes
   - "Cancel" button closes dialog

4. **Modify `DocumentList.tsx`:**
   - Add `onMoveDocument: (docId: string, docName: string) => void` to Props
   - Wrap each document row in a shadcn `ContextMenu`:
     - "Move to Folder..." menu item -> calls `onMoveDocument(doc.id, doc.filename)`
   - Import ContextMenu, ContextMenuTrigger, ContextMenuContent, ContextMenuItem from shadcn
  </action>
  <verify>
    <automated>cd /home/przbadu/dev/Automator/.worktrees/phase-5/frontend && npx tsc --noEmit 2>&1 | head -30 && grep -q "folder_id" src/hooks/useDocuments.ts && grep -q "moveDocument" src/hooks/useDocuments.ts && grep -q "folder_id" src/components/documents/DocumentUpload.tsx && grep -q "MoveToFolderDialog" src/components/documents/MoveToFolderDialog.tsx && grep -q "ContextMenu" src/components/documents/DocumentList.tsx && echo "VERIFY PASS" || echo "VERIFY FAIL"</automated>
  </verify>
  <done>
    - uploadDocument accepts optional folderId and appends it to FormData
    - moveDocument function available from useDocuments hook
    - MoveToFolderDialog renders folder tree for destination selection
    - DocumentList shows context menu with "Move to Folder..." option
    - DocumentUpload shows folder name indicator when uploading to a folder
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire move operations and folder move into DocumentsLayout</name>
  <files>
    frontend/src/components/documents/DocumentsLayout.tsx
    frontend/src/components/documents/FolderTreeItem.tsx
    frontend/src/components/documents/FolderTree.tsx
  </files>
  <action>
1. **Update `DocumentsLayout.tsx`:**
   - Add `moveDocument` to useDocuments destructuring
   - Add MoveToFolderDialog state: `moveDialogState: { open: boolean, itemType: 'document' | 'folder', itemId: string, itemName: string, currentFolderId: string | null } | null`
   - Pass `folderId={selectedFolderId}` and `folderName={selectedFolderName}` to DocumentUpload (derive selectedFolderName from tree by finding the node matching selectedFolderId)
   - Pass `onUpload={(file) => uploadDocument(file, selectedFolderId)}` to DocumentUpload (modified signature)
   - Pass `onMoveDocument={(docId, docName) => setMoveDialogState({ open: true, itemType: 'document', itemId: docId, itemName: docName, currentFolderId: selectedFolderId })}` to DocumentList
   - Render MoveToFolderDialog:
     - When itemType is 'document': onMove calls `moveDocument(itemId, targetFolderId)` then `loadTree()` to refresh document counts
     - When itemType is 'folder': onMove calls `moveFolder(itemId, targetFolderId)`
   - After any move operation succeeds, reload both tree and documents
   - Add "Move to..." option to FolderContextMenu callbacks -- when user clicks "Move to..." on a folder in the tree, open MoveToFolderDialog with itemType='folder'

2. **Update `FolderTreeItem.tsx`:**
   - Add `onMoveFolder: (id: string, name: string) => void` to props
   - Add "Move to..." item in the FolderContextMenu (between Rename and Delete)
   - Clicking "Move to..." calls `onMoveFolder(node.id, node.name)` which will open the MoveToFolderDialog in DocumentsLayout

3. **Update `FolderTree.tsx`:**
   - Pass `onMoveFolder` prop through to FolderTreeItem components

4. **After upload with folder targeting, reload the tree** to update document_count badges.
  </action>
  <verify>
    <automated>cd /home/przbadu/dev/Automator/.worktrees/phase-5/frontend && npx tsc --noEmit 2>&1 | head -30 && grep -q "moveFolder" src/components/documents/DocumentsLayout.tsx && grep -q "MoveToFolderDialog" src/components/documents/DocumentsLayout.tsx && grep -q "onMoveFolder" src/components/documents/FolderTreeItem.tsx && echo "VERIFY PASS" || echo "VERIFY FAIL"</automated>
  </verify>
  <done>
    - Uploading a file with a folder selected places the file in that folder
    - Right-clicking a document shows "Move to Folder..." which opens a folder picker dialog
    - Right-clicking a folder shows "Move to..." which opens the same folder picker dialog
    - After any move, tree and document list refresh to show updated state
    - Document counts in folder tree update after upload and move operations
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete folder management UI</name>
  <files>none</files>
  <action>
    Human verifies the complete folder management UI works end-to-end. All automation is complete in Tasks 1-2. This checkpoint confirms visual correctness and interactive behavior.
  </action>
  <verify>
    <automated>cd /home/przbadu/dev/Automator/.worktrees/phase-5/frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>
    Human confirms all 5 UI requirements work: folder tree sidebar (UI-01), folder CRUD dialogs (UI-02), upload-to-folder (UI-03), document move (UI-04), folder move (UI-05).

    Verification steps:
    1. Start dev server: `bin/dev`
    2. Open http://0.0.0.0:5173 and log in with test@example.com / password123
    3. Navigate to Documents/Ingestion interface
    4. Folder Tree (UI-01): Verify folder tree in left sidebar. Create folder, create subfolder. Click chevrons to expand/collapse.
    5. Folder CRUD (UI-02): Right-click folder -> Rename. Right-click folder -> Delete (confirm dialog).
    6. Upload to Folder (UI-03): Select a folder. Upload a file. Verify file appears in that folder.
    7. Move Document (UI-04): Right-click document -> "Move to Folder..." -> pick folder -> confirm.
    8. Move Folder (UI-05): Right-click folder -> "Move to..." -> pick parent -> confirm.
    9. Verify document counts update after all operations.

    Resume: Type "approved" or describe issues.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc --noEmit`
2. Key links verified:
   - `grep -r "folder_id" frontend/src/hooks/useDocuments.ts` (upload targeting)
   - `grep -r "documents.*move" frontend/src/hooks/useDocuments.ts` (document move API)
   - `grep -r "moveFolder" frontend/src/components/documents/DocumentsLayout.tsx` (folder move wiring)
3. Full e2e flow validated via agent-browser:
   - Login -> Documents page -> folder tree visible
   - Create folder -> upload file to folder -> file appears in folder
   - Move document between folders -> document moves
   - Move folder to different parent -> folder hierarchy updates
   - Delete folder -> confirmation dialog -> folder removed
</verification>

<success_criteria>
- Upload targets selected folder (folder_id sent in FormData)
- Documents can be moved between folders via context menu + folder picker dialog
- Folders can be moved to different parents via context menu + folder picker dialog
- All operations refresh tree and document list to reflect changes
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/05-folder-management-ui/05-02-SUMMARY.md`
</output>

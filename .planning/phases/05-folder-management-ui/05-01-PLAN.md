---
phase: 05-folder-management-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/hooks/useFolders.ts
  - frontend/src/components/documents/FolderTree.tsx
  - frontend/src/components/documents/FolderTreeItem.tsx
  - frontend/src/components/documents/FolderContextMenu.tsx
  - frontend/src/components/documents/CreateFolderDialog.tsx
  - frontend/src/components/documents/DeleteFolderDialog.tsx
  - frontend/src/components/documents/DocumentsLayout.tsx
  - frontend/src/components/ui/alert-dialog.tsx
  - frontend/src/components/ui/context-menu.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Ingestion interface shows a folder tree in the left sidebar with expand/collapse"
    - "User can create a new folder via a dialog"
    - "User can rename an existing folder via a dialog"
    - "User can delete a folder with a confirmation dialog"
    - "Clicking a folder in the tree selects it and highlights it"
    - "Clicking 'All Documents' deselects any folder"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "Folder and FolderTreeNode types, folder_id on Document"
      contains: "FolderTreeNode"
    - path: "frontend/src/hooks/useFolders.ts"
      provides: "Folder CRUD operations, tree state, selected folder"
      exports: ["useFolders"]
    - path: "frontend/src/components/documents/FolderTree.tsx"
      provides: "Tree container with root-level 'All Documents' button"
      exports: ["FolderTree"]
    - path: "frontend/src/components/documents/FolderTreeItem.tsx"
      provides: "Recursive tree node with expand/collapse, selection, context menu"
      exports: ["FolderTreeItem"]
    - path: "frontend/src/components/documents/CreateFolderDialog.tsx"
      provides: "Dialog for creating and renaming folders"
      exports: ["CreateFolderDialog"]
    - path: "frontend/src/components/documents/DeleteFolderDialog.tsx"
      provides: "Alert dialog for delete confirmation"
      exports: ["DeleteFolderDialog"]
    - path: "frontend/src/components/documents/DocumentsLayout.tsx"
      provides: "Two-panel layout: folder tree sidebar + document list"
  key_links:
    - from: "frontend/src/components/documents/FolderTree.tsx"
      to: "/folders/tree"
      via: "useFolders hook"
      pattern: "fetchWithAuth.*folders/tree"
    - from: "frontend/src/components/documents/FolderTreeItem.tsx"
      to: "FolderContextMenu"
      via: "right-click context menu"
      pattern: "ContextMenu"
    - from: "frontend/src/components/documents/DocumentsLayout.tsx"
      to: "FolderTree"
      via: "sidebar rendering"
      pattern: "FolderTree"
---

<objective>
Build the folder tree sidebar with folder CRUD dialogs in the ingestion/documents interface.

Purpose: Give users a navigable folder hierarchy (UI-01) and folder management actions (UI-02) in the documents view.
Output: Folder tree component with expand/collapse, context menu for create/rename/delete, two-panel DocumentsLayout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-folder-management-ui/05-RESEARCH.md

<interfaces>
<!-- Existing types and APIs the executor needs -->

From frontend/src/types/index.ts:
```typescript
export interface Document {
  id: string
  user_id: string
  filename: string
  file_size: number
  mime_type: string
  status: "pending" | "processing" | "converting" | "chunking" | "extracting_metadata" | "embedding" | "completed" | "failed"
  chunk_count: number
  error_message: string | null
  content_hash: string | null
  metadata: Record<string, unknown> | null
  created_at: string
  updated_at: string
}
```

From frontend/src/lib/api.ts:
```typescript
export async function fetchWithAuth(path: string, options?: RequestInit): Promise<Response>
export { API_URL }
```

From frontend/src/hooks/useDocuments.ts:
```typescript
export function useDocuments(): {
  documents: Document[]
  uploading: boolean
  loadDocuments: () => Promise<void>
  uploadDocument: (file: File) => Promise<UploadResult>
  deleteDocument: (id: string) => Promise<void>
}
```

Backend API (already implemented):
```
POST   /folders              { name, parent_id? }    -> FolderResponse
GET    /folders/tree          ?root_id=               -> { tree: FolderTreeNode[] }
PATCH  /folders/{id}          { name }                -> FolderResponse (rename)
PATCH  /folders/{id}/move     { parent_id }           -> FolderResponse
DELETE /folders/{id}                                   -> 204
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, useFolders hook, and shadcn component installation</name>
  <files>
    frontend/src/types/index.ts
    frontend/src/hooks/useFolders.ts
    frontend/src/components/ui/alert-dialog.tsx
    frontend/src/components/ui/context-menu.tsx
    frontend/src/components/ui/dropdown-menu.tsx
  </files>
  <action>
1. **Install shadcn components** (run from `frontend/` directory):
   ```bash
   npx shadcn@latest add alert-dialog context-menu dropdown-menu
   ```

2. **Add types to `frontend/src/types/index.ts`:**
   - Add `folder_id: string | null` field to the existing `Document` interface (backend already returns this field)
   - Add new interfaces:
   ```typescript
   export interface Folder {
     id: string
     user_id: string
     name: string
     parent_id: string | null
     path: string
     created_at: string
     updated_at: string
   }

   export interface FolderTreeNode {
     id: string
     name: string
     path: string
     children: FolderTreeNode[]
     document_count: number
   }
   ```

3. **Create `frontend/src/hooks/useFolders.ts`:**
   - State: `tree: FolderTreeNode[]`, `selectedFolderId: string | null`, `loading: boolean`
   - `loadTree()`: GET /folders/tree -> set tree state
   - `createFolder(name: string, parentId: string | null)`: POST /folders -> reload tree -> return created folder
   - `renameFolder(folderId: string, name: string)`: PATCH /folders/{id} with { name } -> reload tree
   - `deleteFolder(folderId: string)`: DELETE /folders/{id} -> if selectedFolderId was the deleted folder, reset to null -> reload tree
   - `moveFolder(folderId: string, newParentId: string | null)`: PATCH /folders/{id}/move with { parent_id } -> reload tree
   - All API calls use `fetchWithAuth` from `@/lib/api`
   - Return: `{ tree, selectedFolderId, setSelectedFolderId, loading, loadTree, createFolder, renameFolder, deleteFolder, moveFolder }`
  </action>
  <verify>
    <automated>cd /home/przbadu/dev/Automator/.worktrees/phase-5/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Folder and FolderTreeNode types exported from types/index.ts
    - Document interface has folder_id field
    - useFolders hook compiles with all CRUD operations
    - shadcn alert-dialog, context-menu, dropdown-menu components installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Folder tree components, CRUD dialogs, and DocumentsLayout refactor</name>
  <files>
    frontend/src/components/documents/FolderTree.tsx
    frontend/src/components/documents/FolderTreeItem.tsx
    frontend/src/components/documents/FolderContextMenu.tsx
    frontend/src/components/documents/CreateFolderDialog.tsx
    frontend/src/components/documents/DeleteFolderDialog.tsx
    frontend/src/components/documents/DocumentsLayout.tsx
  </files>
  <action>
1. **Create `CreateFolderDialog.tsx`:**
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `onSubmit: (name: string) => Promise<void>`, `initialName?: string` (for rename mode), `title?: string` (defaults to "Create Folder")
   - Uses shadcn `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogFooter`
   - Contains a text input for folder name, pre-filled with `initialName` if provided
   - Submit button text: "Create" or "Rename" based on whether `initialName` is provided
   - Validates name is non-empty before submitting
   - Closes dialog and clears input on successful submit

2. **Create `DeleteFolderDialog.tsx`:**
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `onConfirm: () => Promise<void>`, `folderName: string`
   - Uses shadcn `AlertDialog`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogCancel`, `AlertDialogAction`
   - Shows warning: "Delete folder '{folderName}'? This will remove the folder and all its contents. Documents will become unfiled."
   - "Cancel" and "Delete" buttons

3. **Create `FolderContextMenu.tsx`:**
   - Props: `children: React.ReactNode`, `onCreateSubfolder: () => void`, `onRename: () => void`, `onDelete: () => void`
   - Uses shadcn `ContextMenu`, `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`, `ContextMenuSeparator`
   - Menu items: "New Subfolder", "Rename", separator, "Delete" (with `text-destructive` class)

4. **Create `FolderTreeItem.tsx`:**
   - Props: `node: FolderTreeNode`, `level: number`, `selectedFolderId: string | null`, `onSelect: (id: string) => void`, `onCreateFolder: (parentId: string) => void`, `onRenameFolder: (id: string, currentName: string) => void`, `onDeleteFolder: (id: string, name: string) => void`
   - Renders a button with indentation based on `level` (paddingLeft = `level * 16 + 8` px)
   - Shows ChevronRight icon (rotated 90deg when expanded) if node has children — clicking chevron toggles expand/collapse without changing selection
   - Shows Folder icon (FolderOpen when expanded) from lucide-react
   - Shows node.name (truncated)
   - Shows document_count badge if > 0 (muted, right-aligned)
   - Highlighted (bg-accent) when selected
   - Wrapped in FolderContextMenu with create/rename/delete callbacks
   - When expanded, recursively renders children as FolderTreeItem with level+1

5. **Create `FolderTree.tsx`:**
   - Props: `tree: FolderTreeNode[]`, `selectedFolderId: string | null`, `onSelect: (id: string | null) => void`, `onCreateFolder: (parentId: string | null) => void`, `onRenameFolder: (id: string, currentName: string) => void`, `onDeleteFolder: (id: string, name: string) => void`
   - Header row: "Folders" label + FolderPlus icon button (creates root folder via `onCreateFolder(null)`)
   - "All Documents" button below header — selected when `selectedFolderId === null`, calls `onSelect(null)`
   - Maps over tree nodes rendering FolderTreeItem for each root folder
   - Empty state: show "No folders yet" text if tree is empty

6. **Refactor `DocumentsLayout.tsx`:**
   - Import and use `useFolders` hook alongside existing `useDocuments`
   - Call `loadTree()` in useEffect alongside `loadDocuments()`
   - Replace the current sidebar content (the placeholder text) with the FolderTree component
   - Keep the existing sidebar structure: folder tree in flex-1 area, user email + logout at bottom
   - Filter documents by selectedFolderId: if null show all, if set show only docs where `doc.folder_id === selectedFolderId`
   - Show the selected folder name in the header (e.g., "Documents > FolderName" or just "Documents" for all)
   - Manage dialog state for create/rename/delete: `dialogState` with type discriminator ('create' | 'rename' | 'delete')
   - Render CreateFolderDialog and DeleteFolderDialog controlled by dialogState
   - Pass appropriate callbacks down through FolderTree -> FolderTreeItem that open the dialogs
  </action>
  <verify>
    <automated>cd /home/przbadu/dev/Automator/.worktrees/phase-5/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Folder tree renders in the left sidebar with expand/collapse on nested folders
    - Right-clicking a folder shows context menu with New Subfolder, Rename, Delete options
    - Create/Rename dialog opens with text input, creates/renames folder on submit
    - Delete dialog shows confirmation warning, deletes folder on confirm
    - "All Documents" shows all docs, selecting a folder filters to that folder's docs
    - Selected folder is visually highlighted in the tree
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd frontend && npx tsc --noEmit`
2. Dev server starts: `cd /home/przbadu/dev/Automator/.worktrees/phase-5 && bin/dev` (verify no console errors)
3. Manual verification via agent-browser:
   - Navigate to documents page
   - Verify folder tree sidebar renders
   - Create a folder via the + button
   - Right-click folder -> Rename
   - Right-click folder -> Delete (confirm dialog appears)
   - Create nested folders, verify expand/collapse
   - Select folder, verify document list filters
</verification>

<success_criteria>
- Folder tree sidebar visible in documents/ingestion interface with expand/collapse
- Context menu on folders provides Create Subfolder, Rename, Delete actions
- Create and Rename use dialog with text input
- Delete uses confirmation alert dialog
- Selecting a folder filters the document list to that folder
- All operations call backend APIs and refresh tree on success
</success_criteria>

<output>
After completion, create `.planning/phases/05-folder-management-ui/05-01-SUMMARY.md`
</output>
